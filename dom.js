 const buttons=document.getElementsByClassName("btn-1")
// buttons.forEach(btn => {
// btn.addEventListener("click",function(){


// const coins=coin-20;

// })

// })

const callButtons1 = document.getElementById("btn-1");
    callButtons1.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;
        let sernam=document.getElementById("service-name-1").innerText;
        let sernum=document.getElementById("service-number-1").innerText;
        console.log(coins);
     
        if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
alert("National Emergency 999")
        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;

        const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        
        li2.innerText=sernum;
         const now = new Date();
      const timeString = now.toLocaleTimeString();
      li.innerText=sernam+"  "+timeString;
        div.appendChild(li);
        div.appendChild(li2);

        
      
    }
)
const callButtons2 = document.getElementById("btn-2");
    callButtons2.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;

         let sernam=document.getElementById("service-name-2").innerText;
        let sernum=document.getElementById("service-number-2").innerText;
        console.log(coins);
      
        if (coins < 20) {
         alert("Not enough coins! You need at least 20.");
          return;
        }
 alert("Police helpline number 999")
        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;

               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)
const callButtons3 = document.getElementById("btn-3");
    callButtons3.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;

         let sernam=document.getElementById("service-name-3").innerText;
        let sernum=document.getElementById("service-number-3").innerText;
        console.log(coins);
    
        if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
alert("Fire service number 999")
        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;
               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)
const callButtons4 = document.getElementById("btn-4");
    callButtons4.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;
         let sernam=document.getElementById("service-name-4").innerText;
        let sernum=document.getElementById("service-number-4").innerText;
        console.log(coins);
       
      if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
alert("Ambulance number 999")
        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;
               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)
const callButtons5 = document.getElementById("btn-5");
    callButtons5.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;
         let sernam=document.getElementById("service-name-5").innerText;
        let sernum=document.getElementById("service-number-5").innerText;
        console.log(coins);
    
        if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
         alert("Women and child helpline 1994-999999") 

        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;
               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)
const callButtons6 = document.getElementById("btn-6");
    callButtons6.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;
         let sernam=document.getElementById("service-name-6").innerText;
        let sernum=document.getElementById("service-number-6").innerText;
        console.log(coins);
         

        if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
        alert("Anti corruption helpline 109")

        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;
               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)
const callButtons7 = document.getElementById("btn-7");
    callButtons7.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;
         let sernam=document.getElementById("service-name-7").innerText;
        let sernum=document.getElementById("service-number-7").innerText;
        console.log(coins);
       

        if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
         alert("Electricity helpline 106")

        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;
               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)
const callButtons8 = document.getElementById("btn-8");
    callButtons8.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;
         let sernam=document.getElementById("service-name-8").innerText;
        let sernum=document.getElementById("service-number-8").innerText;
        console.log(coins);
        

        if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
      alert("Brac helpline 16216");
        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;
               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)
const callButtons9 = document.getElementById("btn-9");
    callButtons9.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
        let coins=document.getElementById("coin").innerText;
         let sernam=document.getElementById("service-name-9").innerText;
        let sernum=document.getElementById("service-number-9").innerText;
        console.log(coins);
    
        if (coins < 20) {
          alert("Not enough coins! You need at least 20.");
          return;
        }
     alert("Bangladesh railway 163")
        coins -= 20;
        // coinElement.textContent = coins;
        document.getElementById("coin").innerText=coins;
               const div=document.getElementById("history");

        const li=document.createElement("p");
        const li2=document.createElement("p");
        li.innerText=sernam;
        li2.innerText=sernum;
        div.appendChild(li);
        div.appendChild(li2);
    }
)

    
// this is for love 
const lovebutton = document.getElementById("heart-1");
    lovebutton.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton2 = document.getElementById("heart-2");
    lovebutton2.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton3 = document.getElementById("heart-3");
    lovebutton3.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton4 = document.getElementById("heart-4");
    lovebutton4.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton5 = document.getElementById("heart-5");
    lovebutton5.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton6 = document.getElementById("heart-6");
    lovebutton6.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton7 = document.getElementById("heart-7");
    lovebutton7.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton8 = document.getElementById("heart-8");
    lovebutton8.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 
const lovebutton9 = document.getElementById("heart-9");
    lovebutton9.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
        let loves=document.getElementById("heart").innerText;
       
        console.log(loves);

       

        let count=parseInt(loves)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("heart").innerText=count;
    }
)
// this is for love 


// This is for copy button
// this is for love 
// const copybutton = document.getElementById("copy-1");
    // copybutton.addEventListener("click", function () {
        // const card = this.parentElement;
        // const serviceName = card.querySelector(".service-name").textContent;
        // const serviceNumber = card.querySelector(".service-number").textContent;
    
        const copyBtn = document.getElementById("copy-1");
    copyBtn.addEventListener("click", () => {
      const number = document.getElementById("service-number-1").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
       

    const copyBtn2 = document.getElementById("copy-2");
    copyBtn2.addEventListener("click", () => {
      const number = document.getElementById("service-number-2").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
    const copyBtn3 = document.getElementById("copy-3");
    copyBtn3.addEventListener("click", () => {
      const number = document.getElementById("service-number-3").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
    const copyBtn4 = document.getElementById("copy-4");
    copyBtn4.addEventListener("click", () => {
      const number = document.getElementById("service-number-4").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
    const copyBtn5 = document.getElementById("copy-5");
    copyBtn5.addEventListener("click", () => {
      const number = document.getElementById("service-number-5").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
    const copyBtn6 = document.getElementById("copy-6");
    copyBtn6.addEventListener("click", () => {
      const number = document.getElementById("service-number-6").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
    const copyBtn7 = document.getElementById("copy-7");
    copyBtn7.addEventListener("click", () => {
      const number = document.getElementById("service-number-7").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
    const copyBtn8 = document.getElementById("copy-8");
    copyBtn8.addEventListener("click", () => {
      const number = document.getElementById("service-number-8").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
    const copyBtn9 = document.getElementById("copy-9");
    copyBtn9.addEventListener("click", () => {
      const number = document.getElementById("service-number-9").innerText;
      navigator.clipboard.writeText(number)
        .then(() => {
          alert(`Copied: ${number}`);
        })
        .catch(err => {
          console.error("Failed to copy: ", err);
        });
         let copys=document.getElementById("cop").innerText;

        let count=parseInt(copys)
        count+=1;
        // coinElement.textContent = coins;
        document.getElementById("cop").innerText=count;
    });
const contain=document.getElementById("history");
const btnclr=document.getElementById("clear");

btnclr.addEventListener("click",function(){
    contain.innerHTML="";
})
